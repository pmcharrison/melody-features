# Feature name and implementation audit

This document records candidate issues found during a read-only audit of the
exported feature table generated by `docs/quarto_table_build.py::build_table()`.
The goal is to catch features where the implementation, function name, display
name, docstring, or sign convention may mislead users.

These findings are intentionally conservative: they are not all confirmed bugs,
but each item appears worth triage before relying on the feature name as a
description of the returned value.

## Review method

- The feature table currently exports 246 rows.
- Read-only subagents reviewed bounded slices by category: pitch, interval,
  contour, duration/timing, duration/rhythm-pulse, tonality, complexity, and
  corpus.
- Each subagent compared feature names, display names, docstrings, source code,
  helper implementations, and available tests.
- This report records only questionable cases, not features that looked correct.

## Severity guide

- **High**: likely incorrect computation, inverted sign/convention, crash-prone
  edge case, or a name that materially misrepresents the returned value.
- **Medium**: likely misleading naming, duplicate/alias ambiguity, inconsistent
  code paths, or behavior that needs documentation before users can interpret it.
- **Low**: minor naming/documentation ambiguity, likely harmless convention
  mismatch, or lower-confidence parity concern.

## High-priority shortlist

These should be triaged first because they look like implementation bugs,
inverted signs, or major name/meaning mismatches.

| Feature | Category | Concern |
|---|---|---|
| `polyrhythms`, `polyrhythms_tempo_standardized` | Duration | Appears to return harmonic peak fraction, while docs/name imply non-harmonic fraction. |
| `tonalness_histogram` | Tonality | Histograms only the top correlation scalar instead of all 24 key correlations. |
| `tonal_entropy` | Tonality | Computes entropy over distinct float values, not over a normalized key-correlation distribution. |
| `basic_pitch_histogram` | Pitch | Returns equal-width binned ranges, not pitch-value counts. |
| `melodic_interval_histogram` | Interval | Returns equal-width binned ranges, not interval-size counts. |
| `mean_log_tfdf`, `norm_log_dist` | Corpus | Names/docs imply log TF-DF, but implementation uses linear normalized values. |
| All corpus n-gram order loops | Corpus | `range(1, max_ngram_order)` excludes the configured maximum order. |
| `mobility`, `mean_mobility`, `mobility_std` | Complexity | Per-note mobility appears to use the wrong pitch index. |
| `complebm` | Complexity | Uses signed mean interval where proximity should likely use absolute intervals. |
| `onset_autocorrelation`, `onset_autocorr_peak` | Duration | Quantizes second-based onsets without tempo conversion. |
| `syncopation` | Duration | Possible sign/metrical-weight inversion relative to the documented syncopation definition. |
| `pcdist1` | Pitch | Empty/mismatched input returns `0.0` despite dict return type; duration weighting is coarse. |
| `ioi_range`, `minimum_note_duration` | Duration | Crash-prone on empty or one-note inputs. |

## Findings by category

### Pitch

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `basic_pitch_histogram` | High | Implementation-name mismatch | Docstring promises pitch-value counts, but implementation calls `histogram_bins()`, producing equal-width range bins with string keys. | Replace with per-pitch counting or rename/document as a binned pitch-range histogram. |
| `pcdist1` | High | Return-type bug | Empty/mismatched input returns `0.0` although the annotation and expected shape are `dict`. | Return `{}` consistently and align tests/docs. |
| `importance_of_bass_register`, `importance_of_middle_register`, `importance_of_high_register` | High | Edge-case bug | Divide by `len(pitches)` without an empty-input guard. | Guard empty input or document a raised error. |
| `prevalence_of_most_common_pitch` | High | Edge-case bug | Divides by `len(pitches)` without an empty-input guard. | Guard empty input. |
| `mean_pitch` | Medium | Edge-case bug | `int(np.mean([]))` can yield warnings or errors on empty input. | Define and test empty-input behavior. |
| `pcdist1` | Medium | Approximation/name mismatch | Duration weighting repeats pitches by `max(1, int(duration * 10))`, a coarse unit-dependent proxy. | Weight directly by duration/ticks or document the approximation. |
| `tessitura`, `mean_tessitura` | Medium | Misleading documentation | Description says higher values indicate wider pitch range, but implementation is mean absolute local z-score from a running median. | Reword as local registral deviation, not global range. |
| `pitch_standard_deviation` / `pitch_variability` | Medium | Alias/export ambiguity | The decorated export appears as "Pitch Standard Deviation"; literature/test name is "Pitch Variability", and the alias is not in the table. | Expose or document the alias, or use the literature name as display text. |
| `strong_tonal_centres` | Medium | Documentation ambiguity | Preamble says pitch-class histogram peaks, but implementation uses folded-fifths pitch-class histogram. | Clarify folded-fifths basis. |
| `number_of_common_pitches_classes` | Medium | Display-name typo | Table renders awkwardly as "Number Of Common Pitches Classes". | Rename to `number_of_common_pitch_classes` with alias if needed. |
| `mean_pitch_class` | Low | Semantic ambiguity | Arithmetic mean of pitch classes ignores circular pitch-class geometry. | Document as a linear pitch-class mean or switch to circular mean if intended. |
| `folded_fifths_pitch_class_histogram` | Low | Documentation ambiguity | Dict keys remain pitch-class ids; only insertion order follows circle of fifths. | Clarify key meaning. |
| `melodic_pitch_variety` | Low | Documentation ambiguity | Counts distinct onset ticks rather than strictly notes. | Clarify tick/onset basis. |

### Interval

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `melodic_interval_histogram` | High | Implementation-name mismatch | Docstring promises interval-size counts, but implementation uses equal-width range bins via `histogram_bins()`. | Use semitone-indexed counts or `MelodicIntervalHistogram`. |
| `melodic_octaves` | High | Type mismatch | Annotated `-> int`, but returns a float proportion. | Change annotation/table typing to `float`. |
| `ivdirdist1` | Medium | Misleading documentation | Preamble says proportion of upward intervals; implementation computes directional bias `(up - down) / (up + down)`. | Describe as directional bias and consider clearer public name. |
| `interval_entropy` | Medium | Ambiguous statistic | Docs say interval sizes, but code computes entropy of signed intervals. | Clarify signed vs absolute, or use absolute intervals if size is intended. |
| `mean_absolute_interval` / `mean_melodic_interval` | Medium | Duplicate alias | Same function object exported under two names. | Deduplicate table output or mark one as an alias. |
| `modal_interval` / `most_common_interval` | Medium | Duplicate alias | Same function object exported under two names. | Deduplicate table output or mark one as an alias. |
| `minor_major_third_ratio` | Medium | Misleading sentinel | Returns `0.0` when major-thirds denominator is zero, even if minor thirds exist. | Use `NaN`, `inf`, or document sentinel. |
| `interval_direction_mean` vs `direction_of_melodic_motion` | Low | Documentation ambiguity | Both sound like direction metrics but use different denominators and unison handling. | Cross-reference docstrings and clarify formulas. |
| `interval_direction_std` | Low | Convention mismatch | Uses population standard deviation, while related absolute-interval SD uses sample SD. | Document or align the convention. |

### Contour

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| Interpolation contour properties | High | Source/implementation mismatch | Table labels these FANTASTIC, but `InterpolationContour` defaults to the modified AMADS method; values differ from the FANTASTIC method. | Default to `method="fantastic"` for parity or relabel/document AMADS default. |
| `InterpolationContour.global_direction` | Medium | Misleading name/doc | Computes `sign(sum(gradients))`; balanced up/down contours return 0 due to cancellation, not necessarily flatness. | Clarify as net signed gradient direction. |
| `InterpolationContour.mean_gradient` | Medium | Misleading display name | Name implies signed mean; implementation returns mean absolute gradient. | Rename to `mean_absolute_gradient` or document clearly. |
| `InterpolationContour.direction_changes` | Medium | Documentation ambiguity | Denominator is transitions between distinct consecutive samples, not all interpolated values. | Document denominator exactly. |
| `get_interpolation_contour_features` and sibling wrappers | Medium | Type mismatch | Wrappers are annotated as returning contour classes but return tuples/lists. | Fix annotations/docstrings. |
| `HuronContour.class_label` | Medium | Documentation gap | Weighted mean pitch is rounded before classification, which can flip borderline categories. | Document rounding rule. |
| `StepContour.global_variation` | Low | FANTASTIC parity risk | Uses population SD (`ddof=0`); R/FANTASTIC may use sample SD. | Confirm reference convention. |
| `PolynomialContour.coefficients` | Low | Possible parity risk | Single-pass backward selection may diverge from iterative `stepAIC`. | Cross-validate and iterate until BIC-stable if parity is required. |
| `InterpolationContour.class_label` | Low | Documentation ambiguity | Doc mentions numeric codes but property returns letters. | Clarify that numeric codes are threshold descriptions only. |

### Duration and timing

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `length` | High | Misleading name / duplicate | Display name reads like elapsed time, but implementation returns note count, same as `total_number_of_notes`. | Rename in docs/table or mark as FANTASTIC note-count alias; return type should be `int`. |
| `modal_duration` vs `most_common_rhythmic_value` | High | Name/algorithm ambiguity | Both sound like most common duration, but one uses raw durations and one uses rhythmic-value histogram bins. | Clarify which matches jSymbolic and rename the other. |
| `mean_duration`, `mean_rhythmic_value`, `average_note_duration` | High | Unit/algorithm confusion | Names all imply average duration, but use quarter-note raw values, histogram ideal bin values, and seconds respectively. | Add units to display names and docstrings. |
| `amount_of_staccato` | High | Misleading name | Counts notes shorter than 0.1 seconds; does not inspect staccato articulation. | Rename to a short-note fraction or document the cutoff prominently. |
| `ioi_range` | High | Edge-case bug | Calls `max()`/`min()` on an empty interval list for 0- or 1-note inputs. | Return `0.0` or define sentinel for no IOIs. |
| `minimum_note_duration` | High | Edge-case bug | Calls `min([])` on empty input. | Guard empty inputs and fix docstring parameters. |
| `global_duration` / `duration_in_seconds` | Medium | Duplicate alias | Comment says alias should not return again, but feature collection can pick up both names. | Filter aliases in `_get_features_by_type()` / `build_table()`. |
| `ioi_mean` / `average_time_between_attacks` | Medium | Duplicate alias | Same function object exported under two names. | Deduplicate or mark alias. |
| `ioi_standard_deviation` / `variability_of_time_between_attacks` | Medium | Duplicate alias | Same function object exported under two names. | Deduplicate or mark alias. |
| `total_number_of_notes` | Medium | Duplicate ambiguity | Same computation as `length`. | Keep one canonical export or document aliasing. |
| `shortest_rhythmic_value`, `longest_rhythmic_value` | Medium | Misleading name | Return ideal values for occupied histogram bins, not actual min/max note durations. | Rename as quantized rhythmic categories. |
| `note_density_per_quarter_note` | Medium | Edge-case inconsistency | Returns 0 for single-note melodies despite a definable density. | Align guard with `note_density`. |
| `prevalence_of_short/medium/long_rhythmic_values` | Medium | Documentation ambiguity | Category bins overlap; sums can exceed 1.0. | Document non-exclusive/nested bin membership. |
| `complete_rests_fraction` and complete-rest stats | Medium | Inconsistent threshold | Fraction uses all silent runs; other rest stats use a 0.1 QN threshold. | Document or align thresholds. |
| `ioi_contour` and summaries | Low | Documentation ambiguity | Requires at least three onsets because it is based on IOI ratios, not raw IOIs. | Document minimum note count and ratio basis. |
| `equal_duration_transitions`, `half_duration_transitions`, `dotted_duration_transitions` | Low | Misleading signature | `tempo` parameter appears unused. | Remove or document unused parameter. |

### Duration, meter, pulse, and syncopation

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `polyrhythms` | High | Sign/convention inversion | Docs imply fraction of peaks not harmonically related to strongest pulse; code appears to count related peaks. | Return the complement or update docs/name. |
| `polyrhythms_tempo_standardized` | High | Sign/convention inversion | Same inverted formula in tempo-standardized path. | Same as above. |
| `onset_autocorrelation` | High | Tempo omission | Quantizes second-based onsets as if seconds were quarter notes. | Convert seconds to quarter-note time using tempo before quantization. |
| `onset_autocorr_peak` | High | Inherited bug | Delegates to `onset_autocorrelation`, inheriting tempo mis-quantization. | Fix upstream and test at non-60 BPM. |
| `syncopation` | High | Possible sign/weight inversion | Positive syncopation appears to require rest weight greater than preceding note weight under the current encoding, possibly opposite the documented Longuet-Higgins/Lee interpretation. | Reconcile metric-weight direction and add known-pattern tests. |
| `number_of_unique_time_signatures` | Medium | Implementation-name mismatch | Counts unique `(time, numerator, denominator)` tuples, so repeated same signature at different times counts multiple times. | Count unique `(numerator, denominator)` pairs. |
| `harmonicity_of_two_strongest_rhythmic_pulses` | Medium | Pairing ambiguity | Uses raw max peak and threshold-table second peak, which can disagree with standalone second-strongest pulse. | Document hybrid jSymbolic logic or align peak selection. |
| `harmonicity_of_two_strongest_rhythmic_pulses_tempo_standardized` | Medium | Pairing ambiguity | Same issue in standardized path. | Same as above. |
| `number_of_strong_rhythmic_pulses` and standardized variant | Medium | Documentation mismatch | Doc says threshold `> 0.001`, but effective column gate appears to be `> 0.1`. | Fix docs or threshold logic. |
| `tempo_variability` | Medium | Misleading measure | Uses unweighted SD of tempo-change BPM values, ignoring duration spent at each tempo. | Narrow docstring or duration-weight tempo segments. |
| `syncopicity` | Medium | Documentation ambiguity | Returns sum of per-level proportions, not a raw count, and grid alignment ignores later meter/tempo changes. | Clarify scale and alignment assumptions. |

### Tonality

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `tonalness_histogram` | High | Implementation-name mismatch | Doc promises histogram of 24 key correlations; code histograms only the top correlation scalar. | Pass all 24 correlation values to the histogram. |
| `tonal_entropy` | High | Incorrect statistic | Entropy is computed over unique float values rather than a normalized 24-key correlation distribution. | Normalize correlation magnitudes or use a defined probability transform first. |
| `referent`, `inscale`, `proportion_inscale`, scalar-passage features | High | Key-estimation inconsistency | These always infer key from pitch content and ignore MIDI key signature / `key_estimation`, unlike `key()` and `mode()`. | Add `key_estimation` support or document always-inferred behavior. |
| `tonalness` | Medium | Sign/doc mismatch | Doc says magnitude, function returns signed top correlation; batch path uses absolute value. | Use `abs()` or document signed return consistently. |
| `key` | Medium | Format inconsistency | Inferred and MIDI-read keys can use different string formats. | Normalize to one canonical key string. |
| `tonal_clarity`, `tonal_spike` | Medium | Sentinel/doc ambiguity | Docs and code disagree on low-correlation sentinels; empty input can look maximally clear. | Define explicit empty/zero-correlation behavior. |
| `proportion_conjunct_scalar`, `proportion_scalar` | Medium | Misleading name/doc | Implementation is longest passage length divided by total notes, not the fraction of notes in all scalar passages. | Rename or rewrite docs/formula. |
| `referent` | Medium | Misleading display | Returns pitch-class offset from C, not a note name. | Display as tonic pitch class/semitones above C. |
| `tonal_tension` | Medium | API/doc inconsistency | Does not expose `key_estimation` like sibling stats; dict keys differ from doc wording. | Add parameter and document actual keys. |
| Shadowed `referent` / `inscale` definitions | Low | Dead duplicate definitions | Earlier decorated definitions are shadowed by later ones. | Remove or rename shadowed definitions. |

### Complexity

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `mobility`, `mean_mobility`, `mobility_std` | High | Implementation bug | Uses `pitches[i-2]` where parallel tessitura logic uses `pitches[i-1]`; values may attach to wrong notes. | Correct index and test against reference. |
| `NGramCounter.total_tokens` consumers | High | Implementation-name mismatch | `total_tokens` sums all n-gram hit counts across orders, not underlying token count; affects Yule's K, Simpson's D, Honore's H, entropy. | Define N as unigram token count or compute per-order stats. |
| `mean_productivity` | High | Doc/implementation mismatch | Doc says hapax types divided by total tokens; code returns hapax types divided by all types. | Implement `v1 / n` or rewrite doc. |
| `complebm` | High | Formula/sign mismatch | Uses signed mean intervals; proximity should likely use mean absolute interval size. | Use absolute intervals or document deviation. |
| `mean_entropy` | Medium | Misleading name/doc | Returns a single pooled n-gram entropy, not clearly a mean over orders; inherits incorrect N. | Clarify or compute per-order mean entropy. |
| `narmour_registral_direction` | Medium | Documentation mismatch | Doc threshold wording differs from code/IDyOM thresholds for large/small intervals. | Fix docstring thresholds. |
| `simpsons_d` | Medium | Sign/name ambiguity | Described as diversity, but formula is concentration/repetition; higher means less diverse. | Rename/clarify as concentration. |
| `gradus` | Medium | Type/rounding mismatch | Doc promises mean gradus, but code truncates to `int`. | Return `float` or document rounding. |
| N-gram warning messages | Medium | Misleading warnings | Messages say sequence length <= 1, but condition is number of distinct n-gram types <= 1. | Reword warnings. |
| `melodic_attraction` | Medium | Formula uncertainty | Custom scaling/clamp and a dead branch make reference fidelity unclear. | Validate against MIDI Toolbox/reference examples. |
| `narmour_closure` | Low | Doc imprecision | Code allows final interval at least one tone smaller, while wording says more than one tone. | Tighten docstring. |
| `narmour_intervallic_difference` | Low | Documentation ambiguity | Code uses tolerance bands; doc says same size. | Document tolerance bands. |
| `chromatic_motion`, `stepwise_motion`, `repeated_notes` helper docs | Low | Helper doc mismatch | Helpers say proportion of notes but divide by interval count; wrappers are clearer. | Align helper docs. |
| `mean_melodic_accent`, `melodic_accent_std` | Low | Category/API ambiguity | Table categorizes as Complexity, but `get_contour_features()` also emits them. | Pick/document canonical category. |
| `compltrans`, `complebm` | Low | Opaque display names | Table display names are not human-readable. | Add display aliases such as transition/originality complexity. |

### Corpus

| Feature | Severity | Issue type | Concern | Recommended action |
|---|---|---|---|---|
| `mean_log_tfdf` | High | Implementation-name mismatch | Name/doc promise log TF-DF; implementation uses linear normalized TF and DF with no log step. | Apply the intended log/standardization pipeline or rename. |
| `norm_log_dist` | High | Implementation-name mismatch | Name/doc say normalized log distance; implementation computes linear normalized distance. | Apply log transform or rename. |
| All corpus features using n-gram loops | High | Off-by-one bug | `range(1, max_ngram_order)` excludes the configured maximum order. | Use `range(1, max_ngram_order + 1)`. |
| `max_log_df`, `min_log_df`, `mean_log_df` | Medium | Log-base/definition mismatch | Use `np.log1p` natural log while related code/reference appears to use log2. | Confirm reference and align log base. |
| Global/local weight features | Medium | Documentation/sign ambiguity | Documentation is internally inconsistent about whether high weights mean more or less informative/distinctive. | Reconcile with FANTASTIC terminology. |
| Corpus stats vs feature tokenization | Medium | Config mismatch | Corpus stats hardcode `phrase_gap=1.5`, while feature extraction honors configurable `phrase_gap`. | Include phrase gap and max order in corpus-stat generation/cache. |
| Runtime vs standalone weight features | Medium | Inconsistent code paths | Standalone weight functions and `get_corpus_features()` differ in df=0 handling. | Share one implementation. |
| `tfdf_spearman`, `tfdf_kendall` | Low | Reference deviation | Tie handling and df=0 exclusions may differ from FANTASTIC. | Document or match reference tie policy. |
| Corpus tests | Low | Test/doc drift | Tests refer to stale names such as `mean_document_frequency`. | Update tests/docs to current exported names. |
| `build_table()` filter | Low | Table-generation quirk | `get_*` helpers are skipped; table lists atomic features only. | Document this convention if desired. |


## Reference provenance audit

The tables below add a provenance pass for the flagged items. Status values mean:

- **Present upstream**: the same naming, documentation, formula, or convention issue
  appears in the original source/specification.
- **Diverges from upstream**: this package appears to differ from the reference
  implementation or reference documentation.
- **Local/API issue**: the issue is caused by this package's wrappers, table export,
  aliases, edge-case handling, or documentation rather than by a reference feature.
- **No supplied reference**: no reference implementation was supplied for the feature,
  or the feature is labelled novel/custom.
- **Needs deeper validation**: a reference exists, but confirming equivalence requires
  running reference code or checking examples beyond static inspection.

References inspected locally:

- jSymbolic2: `.reference-audit/jSymbolic2`
- Partitura: `.reference-audit/partitura`
- MIDI Toolbox 1.1: `.reference-audit/miditoolbox-1.1`
- FANTASTIC R source: `.reference-audit/fantastic-drive`
- FANTASTIC PDF text: `.reference-audit/FANTASTIC_docs.txt`
- SIMILE PDF text: `.reference-audit/SIMILE_algo_docs_0.3.txt`

### Pitch provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `basic_pitch_histogram` | jSymbolic2 | **Diverges from upstream** | jSymbolic `BasicPitchHistogramFeature` defines a 128-dimensional normalized vector, one bin per MIDI pitch. This package returns equal-width range bins. |
| `pcdist1` | MIDI Toolbox | **Diverges from upstream** | MIDI Toolbox `pcdist1.m` returns a 12-component duration-accent-weighted pitch-class vector. The package's scalar `0.0` empty return and coarse duration repetition are local deviations. |
| `importance_of_bass_register`, `importance_of_middle_register`, `importance_of_high_register` | jSymbolic2 | **Local/API issue** | The concept is upstream jSymbolic, but the empty-list division risk is a Python edge-case handling issue. |
| `prevalence_of_most_common_pitch` | jSymbolic2 | **Local/API issue** | Upstream jSymbolic computes from prepared histograms; the empty-list guard is local Python robustness. |
| `mean_pitch` | jSymbolic2 | **Local/API issue** | jSymbolic has the feature, but the empty-list `np.mean` behavior is local. |
| `tessitura`, `mean_tessitura` | MIDI Toolbox / von Hippel | **Mostly local documentation issue** | MIDI Toolbox describes tessitura as deviation from median pitch height, not global range. The misleading "wider range" wording is local. |
| `pitch_standard_deviation` / `pitch_variability` | jSymbolic2 / FANTASTIC | **Local table/API issue** | Reference names include jSymbolic `Pitch Variability`; this package computes the standard deviation but exports the title-cased implementation name rather than the literature alias. |
| `strong_tonal_centres` | jSymbolic2 | **Local documentation issue** | jSymbolic explicitly says fifths pitch histogram; the package implementation follows that, but the local preamble says pitch-class histogram. |
| `number_of_common_pitches_classes` | jSymbolic2 | **Local table/API issue** | jSymbolic uses `NumberOfCommonPitchClassesFeature`; the awkward pluralized display name comes from the local identifier typo. |
| `mean_pitch_class` | jSymbolic2 | **Present upstream / semantic caveat** | jSymbolic treats pitch classes as linear values. The circular-statistics caveat is inherent to this feature family rather than a divergence. |
| `folded_fifths_pitch_class_histogram` | jSymbolic2 | **Local representation issue** | jSymbolic returns a vector ordered by fifths. This package returns a dict keyed by pitch-class ids, so the key-order ambiguity is local. |
| `melodic_pitch_variety` | jSymbolic2 | **Present upstream / needs validation** | jSymbolic computes from MIDI tick/onset representations. The tick-vs-note wording appears inherited in spirit, but exact parity was not run. |

### Interval provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `melodic_interval_histogram` | jSymbolic2 | **Diverges from upstream** | jSymbolic `MelodicIntervalHistogramFeature` is a 128-bin normalized vector indexed by absolute semitone interval. This package returns equal-width range bins. |
| `melodic_octaves` | jSymbolic2 | **Local type annotation issue** | jSymbolic defines this as a one-dimensional fraction. The package's returned value matches that idea; the `int` annotation is local. |
| `ivdirdist1` | MIDI Toolbox | **Present upstream** | MIDI Toolbox `ivdirdist1.m` doc says proportion of upward intervals, but code computes `(up - down) / (up + down)`. The package mirrors the reference behavior and ambiguity. |
| `interval_entropy` | FANTASTIC | **Likely present upstream** | FANTASTIC computes interval entropy over signed interval values in `Feature_Value_Summary_Statistics.R`; the "sizes" wording is the misleading part. |
| `mean_absolute_interval` / `mean_melodic_interval` | FANTASTIC / jSymbolic2 | **Local table/API issue** | The duplicate table rows come from local alias export across source vocabularies. The metric exists under different reference names. |
| `modal_interval` / `most_common_interval` | FANTASTIC / jSymbolic2 | **Local table/API issue** | Same alias-export issue as above. |
| `minor_major_third_ratio` | jSymbolic2 | **Present upstream** | jSymbolic sets the ratio to 0 if there are no minor thirds or no major thirds. The misleading sentinel is inherited. |
| `interval_direction_mean` vs `direction_of_melodic_motion` | jSymbolic2 / local | **Local documentation issue** | Both are defensible but need local cross-references because their denominators differ. |
| `interval_direction_std` | Local | **No supplied reference** | This exact statistic is not a clear supplied-reference feature; the population-vs-sample choice is local. |

### Contour provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| Interpolation contour properties | FANTASTIC | **Diverges from upstream by default** | FANTASTIC `line.contour()` implements the FANTASTIC turning-point method. This package's `InterpolationContour` defaults to `method="amads"`, so the table's FANTASTIC label is misleading unless the default changes or the source label is broadened. |
| `InterpolationContour.global_direction` | FANTASTIC | **Present upstream** | FANTASTIC computes `sign(sum(int.cont.gradients))`; cancellation behavior is inherited. |
| `InterpolationContour.mean_gradient` | FANTASTIC | **Present upstream** | FANTASTIC computes `mean(abs(int.cont.gradients))`; the local display name hides the absolute-value convention. |
| `InterpolationContour.direction_changes` | FANTASTIC | **Present upstream** | FANTASTIC divides sign-change count by `sum(abs(sign(diff(int.cont.gradients))))`; the denominator nuance is inherited. |
| `get_interpolation_contour_features` and sibling wrappers | Local | **Local/API issue** | Incorrect return annotations are local wrapper issues, not reference behavior. |
| `HuronContour.class_label` | FANTASTIC | **Present upstream with implementation differences** | FANTASTIC rounds the mean pitch before classification. This package also rounds, but uses duration-weighted mean whereas FANTASTIC uses the mean of interior pitches. |
| `StepContour.global_variation` | FANTASTIC | **Diverges from upstream** | FANTASTIC uses R `sqrt(var(...))`, i.e. sample SD. The package uses NumPy population SD by default. |
| `PolynomialContour.coefficients` | FANTASTIC | **Diverges from upstream / needs validation** | FANTASTIC uses `stepAIC(..., direction="both")`; this package uses a simplified backward BIC search. Static inspection suggests possible divergence. |
| `InterpolationContour.class_label` | FANTASTIC | **Present upstream / local docs** | FANTASTIC returns letters from reduced gradient classes. Any expectation of numeric output is a local documentation ambiguity. |

### Duration and timing provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `length` | FANTASTIC | **Present upstream / local display risk** | FANTASTIC `len` is phrase length in notes. The misleading generic display name is inherited from a terse reference name and local title-casing. |
| `modal_duration` vs `most_common_rhythmic_value` | FANTASTIC / jSymbolic2 | **Reference vocabulary collision** | FANTASTIC uses raw duration mode; jSymbolic uses rhythmic-value histogram bins. The local problem is exporting both with similar names without explaining the reference distinction. |
| `mean_duration`, `mean_rhythmic_value`, `average_note_duration` | FANTASTIC / jSymbolic2 | **Reference vocabulary collision / local docs** | These reflect different reference traditions and units. The local table should expose units and algorithm family. |
| `amount_of_staccato` | jSymbolic2 | **Present upstream** | jSymbolic defines this exactly as notes shorter than 0.1 seconds divided by total notes. The articulation-like name is inherited. |
| `ioi_range`, `minimum_note_duration`, `note_density_per_quarter_note` | Local | **Local/API issue** | The edge-case behavior is due to local Python implementations. No supplied reference requires the crash/sentinel behavior. |
| `global_duration` / `duration_in_seconds` | FANTASTIC / jSymbolic2 | **Local table/API issue** | The alias duplication is local. Both reference vocabularies have duration concepts, but the duplicate export is not an upstream requirement. |
| `ioi_mean` / `average_time_between_attacks` | jSymbolic2 / local | **Local table/API issue** | The duplicate alias export is local. |
| `ioi_standard_deviation` / `variability_of_time_between_attacks` | jSymbolic2 / local | **Local table/API issue** | The duplicate alias export is local. |
| `total_number_of_notes` | jSymbolic2 / local | **Local table/API issue** | The duplicate with FANTASTIC `length` is a local aggregation/table issue. |
| `shortest_rhythmic_value`, `longest_rhythmic_value` | jSymbolic2 | **Present upstream / local docs** | jSymbolic rhythmic-value features are histogram-bin/category based; the package should document that these are quantized values, not raw min/max durations. |
| Rhythmic-value prevalence bins | jSymbolic2 | **Likely present upstream** | The bin/category definitions come from jSymbolic rhythmic-value histograms. The local docs should note overlap/nesting if retained. |
| `complete_rests_fraction` and complete-rest stats | jSymbolic2 / local | **Needs deeper validation** | jSymbolic has complete-rest features, but the exact threshold split needs runtime/spec confirmation. Treat as local until confirmed. |
| `ioi_contour` and summaries | Local / IDyOM-style | **No supplied reference** | No supplied source directly defines this ratio-contour API. |
| `equal_duration_transitions`, `half_duration_transitions`, `dotted_duration_transitions` | FANTASTIC | **Local/API issue** | FANTASTIC has duration-transition features; the unused `tempo` parameter is local. |

### Duration, meter, pulse, and syncopation provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `polyrhythms`, `polyrhythms_tempo_standardized` | jSymbolic2 | **Present upstream** | jSymbolic docs say non-harmonic peaks, but the Java implementation counts harmonic factor/multiple hits and returns `hits / peak_bins.length`. The package appears to reproduce the implementation, including the docs/code inversion. |
| `onset_autocorrelation`, `onset_autocorr_peak` | MIDI Toolbox | **Diverges from upstream** | MIDI Toolbox `onsetacorr.m` quantizes `onset(nmat)` in quarter-note units. This package applies the same grid formula to second-based starts without tempo conversion. |
| `syncopation` | Longuet-Higgins/Lee / local | **Needs deeper validation** | The supplied sources do not include the original LH implementation. Static inspection suggests a possible local weight-direction issue, but this needs examples. |
| `number_of_unique_time_signatures` | jSymbolic2 | **Diverges from upstream** | jSymbolic `MetricalDiversityFeature` counts unique numerator/denominator strings; this package counts full `(time, numerator, denominator)` tuples. |
| `harmonicity_of_two_strongest_rhythmic_pulses` and standardized variant | jSymbolic2 | **Likely present upstream** | jSymbolic uses thresholded beat-histogram tables for this family. The local hybrid peak-selection concern may reflect the reference algorithm; exact parity should be checked with examples. |
| `number_of_strong_rhythmic_pulses` and standardized variant | jSymbolic2 | **Likely present upstream / doc ambiguity** | jSymbolic threshold-table columns create effective gates; the local doc should match the actual column semantics. |
| `tempo_variability` | jSymbolic2 / local | **Needs deeper validation** | jSymbolic has a tempo variability feature, but static inspection did not confirm whether duration weighting is expected. |
| `syncopicity` | SIMILE | **Present upstream** | SIMILE docs define syncopicity as per-level syncopations divided by number of notes, summed across levels. The non-count scale is inherited; local docs should be explicit. |

### Tonality provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `tonalness` | FANTASTIC | **Mostly present upstream, with local inconsistency** | FANTASTIC uses the maximum signed correlation (`max(tonality.vector)`), not an absolute value. The local issue is that standalone and batch paths disagree about signed vs absolute values. |
| `tonal_clarity`, `tonal_spike` | FANTASTIC | **Mostly present upstream, with local edge cases** | FANTASTIC defines `A0/A1` and `A0/sum(positive correlations)`. Empty/zero-correlation sentinel behavior is local. |
| `tonalness_histogram` | Local | **No supplied reference** | No inspected reference defines this exported histogram; the single-scalar histogram is local. |
| `tonal_entropy` | Local | **No supplied reference** | No inspected reference defines this exact entropy feature; the probability-distribution issue is local. |
| `referent`, `inscale`, `proportion_inscale`, scalar-passage features | Local / FANTASTIC-inspired | **Local/API issue** | The provided FANTASTIC source computes tonalness/clarity/spike/mode, not the local key-signature-aware API. Ignoring `key_estimation` is local. |
| `key`, `mode` | FANTASTIC / MIDI Toolbox-inspired | **Local/API issue** | References infer mode/key differently; inconsistent string formatting between MIDI-read and inferred paths is local. |
| `proportion_conjunct_scalar`, `proportion_scalar` | Local | **No supplied reference** | No supplied implementation for these exact longest-passage proportions was identified. |
| `tonal_tension` and cloud/momentum/strain summaries | Partitura | **Local wrapper issue** | Partitura returns a structured array with onset-unit-dependent key and tension fields. The missing `key_estimation` parameter and dict-key documentation mismatch are local wrapper/documentation issues. |
| Shadowed `referent` / `inscale` definitions | Local | **Local/API issue** | Dead duplicate definitions are local. |

### Complexity provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `mobility`, `mean_mobility`, `mobility_std` | MIDI Toolbox | **Needs deeper validation / likely divergence** | MIDI Toolbox `mobility.m` uses lag-one correlation and previous-pitch deviations. The package's `pitches[i-2]` indexing does not clearly match the intended formula; run examples before fixing. |
| `NGramCounter.total_tokens` consumers | FANTASTIC | **Diverges from upstream** | FANTASTIC computes m-type statistics per n-gram order, with `N` as the count total for that order, then averages across orders. This package pools all n-gram orders and sums all counts. |
| `mean_productivity` | FANTASTIC | **Diverges from upstream** | FANTASTIC computes per-order singleton-token count divided by total count for that order, then averages. This package returns hapax types divided by all types. |
| `mean_entropy` | FANTASTIC | **Diverges from upstream** | FANTASTIC computes normalized entropy per n-gram order and averages. This package computes one pooled count entropy. |
| `simpsons_d`, `yules_k`, `honores_h`, `sichels_s` | FANTASTIC | **Diverges from upstream** | FANTASTIC formulas are per-order and average across orders. The package uses pooled n-gram counts; Simpson's concentration naming caveat remains conceptual. |
| `complebm` | MIDI Toolbox | **Present upstream** | MIDI Toolbox `complebm.m` uses `mean(diff(pitch(nmat)))`, i.e. signed mean interval, in both pitch and optimal methods. The sign/cancellation concern is inherited from the implementation, even if surprising. |
| `narmour_registral_direction`, `narmour_closure`, `narmour_intervallic_difference` | IDyOM/Narmour | **No supplied reference** | The user-supplied references do not include IDyOM source or Narmour reference code. Only local docs/code were inspected. |
| `gradus` | MIDI Toolbox | **Diverges from upstream / local rounding** | MIDI Toolbox `gradus.m` returns gradus values; this package's truncation to `int(np.mean(...))` is local. |
| N-gram warning messages | Local | **Local/API issue** | Warning wording is local. |
| `melodic_attraction` | MIDI Toolbox | **Needs deeper validation** | MIDI Toolbox `melattraction.m` exists; static inspection found enough differences that example-based comparison is needed. |
| `chromatic_motion`, `stepwise_motion`, `repeated_notes` helper docs | jSymbolic2 / local | **Local documentation issue** | jSymbolic defines these as interval fractions; the local wrapper docs are clearer than helper docs. |
| `mean_melodic_accent`, `melodic_accent_std` | MIDI Toolbox / local | **Local table/API issue** | The dual category/export behavior is local. |
| `compltrans`, `complebm` display names | MIDI Toolbox | **Local table issue** | The terse names come from reference function names, but the table could provide readable aliases. |

### Corpus provenance

| Feature(s) | Nearest reference | Provenance status | Notes |
|---|---|---|---|
| `mean_log_tfdf` | FANTASTIC | **Diverges from upstream** | FANTASTIC `M-Type_Corpus_Features.R` uses `logb(..., 2)` normalized TF and DF before multiplying. This package uses linear normalized values. |
| `norm_log_dist` | FANTASTIC | **Diverges from upstream** | FANTASTIC uses the same log2-normalized TF/DF vectors before the L1 distance. This package uses linear values. |
| Corpus n-gram order loops | FANTASTIC | **Diverges from upstream** | FANTASTIC loops `for(i in min.n:max.n)`, inclusive. This package's `range(1, max_ngram_order)` excludes the maximum order. |
| `max_log_df`, `min_log_df`, `mean_log_df` | FANTASTIC | **Diverges from upstream** | FANTASTIC uses `logb(..., 2)`. This package uses `np.log1p`, a natural-log-plus-one transform. |
| Global/local weight features | FANTASTIC | **Needs deeper validation** | FANTASTIC source defines global/local weights in `M-Type_Corpus_Features.R`; this package's documentation and df=0 handling need example-based comparison. |
| Corpus stats vs feature tokenization | Local | **Local/API issue** | The hardcoded corpus-stat `phrase_gap` is local. |
| Runtime vs standalone weight features | Local | **Local/API issue** | Differing standalone and batch code paths are local. |
| `tfdf_spearman`, `tfdf_kendall` | FANTASTIC | **Diverges partially from upstream** | FANTASTIC ranks with `ties.method="min"`; SciPy uses its own tie handling. The high-level correlation concept is upstream. |
| Corpus tests and `build_table()` filter | Local | **Local table/test issue** | Stale test names and `get_*` table filtering are local. |

## Suggested next steps

1. Triage high-severity items into confirmed bug, intended behavior needing docs,
   or false positive.
2. Add focused tests before changing behavior. Good first tests are:
   - polyrhythms complement/sign convention,
   - tonalness histogram over 24 correlations,
   - melodic and pitch histogram return shapes,
   - corpus max-order inclusion,
   - onset autocorrelation tempo sensitivity.
3. Decide an alias policy for duplicate rows. A small table-builder filter could
   remove duplicate function objects while preserving documented aliases.
4. Consider adding explicit display-name metadata rather than deriving all public
   names from `snake_case.title()`. That would address opaque names such as
   `complebm`, typo-derived names, and unit-bearing duration names.
